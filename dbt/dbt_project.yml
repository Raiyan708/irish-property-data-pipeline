name: "ppr_pipeline"
version: "1.0.0"
config-version: 2

profile: "ppr_pipeline"

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  ppr_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table
